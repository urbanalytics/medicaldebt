PROJECTDIR <- file.path(Sys.getenv("HOME"), "tyyq", "social", "medicaldebt")
DATAROOT <- file.path(Sys.getenv("HOME"), "tyyq", "andmebaasiq", "medical-collection")
LOGSDIR <- file.path(PROJECTDIR, "logs")
                           # logs for manually run stuff under your username
BINDIR <- file.path(PROJECTDIR, "bin")
CODEDIR <- file.path(PROJECTDIR, "code")
## DATA
DATADIR <- file.path(DATAROOT, "casefiles")
TXTDIR <- file.path(DATAROOT, "txt")
RESULTSDIR <- file.path(DATAROOT, "results")
STAGINGDIR <- file.path(DATAROOT, "staging")
